'use strict';

/**
 * @ngdoc function
 * @name angularWpApp.controller:homeCtrl
 * @description
 * # homeCtrl
 * Controller of the angularWpApp
 */
var adSenseTpl = '';

var API_URL = 'http://exosphe.re'

angular.module('angularWpApp')
    .controller('FaqCtrl', function ($scope, $rootScope, $routeParams, $route, wp ) {

        console.log("FaqCtrl");

    })